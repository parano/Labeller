<h2>Tasks</h2>
<table class="table table-striped">
  <thead>
    <tr>
      <th>name</th>
      <th>Status</th>
      <th>Initiated by</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @labeltasks.each do |task| %>
      <tr>
        <% job = Labeljob.find(task.labeljob_id) %>
        <td><%= link_to job.name, labeltask_path(task) %></td>
        <% if task.status == 1 %>
        <td>Unfinished</td>
        <% else %>
        <td>Finished</td>
        <% end %>
        <td><%= user_email(job.user_id) %></td>
        <td>
          <% if task.status == 1 %>
            <%= link_to 'Finish', finish_labeltask_path(task), :class => 'btn btn-mini' %>
          <% else %>
            <%= link_to 'Unfinish', unfinish_labeltask_path(task), :class => 'btn btn-mini' %>
          <% end %>
        </td>
      </tr>
    <% end %>

  </tbody>
</table>