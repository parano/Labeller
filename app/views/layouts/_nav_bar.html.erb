<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container-fluid">
      <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <a class="brand" href="/">Labeller</a>
      <div class="container-fluid nav-collapse">
        <ul class="nav">
          <li><%= link_to "Jobs", labeljobs_path if current_user && !current_user.labeller? %></li>
          <li><%= link_to "Tasks", root_url  %></li>
        </ul>

        <% if current_user %>
          <ul class="nav secondary-nav pull-right" id="userbar">
            <li><a href="<%= edit_user_registration_path %>"><%= current_user.role %>: <%= current_user.email %></a></li>
            <li><%= link_to "exit", destroy_user_session_path, method: :delete %></li>
          </ul>
        <% else %>
          <ul class="nav secondary-nav pull-right" id="userbar">
            <li<%=' class=active' if params[:controller] == "devise/registrations" and params[:action] == "new" %>><a href="<%= new_user_registration_path %>">Register</a></li>
            <li<%=' class=active' if params[:controller] == "devise/sessions" and params[:action] == "new" %>><a href="<%= new_user_session_path %>">login</a></li>
          </ul>
        <% end %>
      </div><!--/.nav-collapse -->

    </div>
  </div>
</div>