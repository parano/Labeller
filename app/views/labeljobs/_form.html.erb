<%= form_for @labeljob, :html => { :class => 'form-horizontal' } do |f| %>
  <fieldset>
    <legend><%= controller.action_name.capitalize %> Labeljob</legend>

    <div class="control-group">
      <%= f.label :name, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :name, :class => 'text_field' %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :desc, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_area :desc, :class => 'text_area' %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :deadline, :class => 'control-label' %>
      <div class="controls">
        <%= f.date_select :deadline, :class => 'date_select' %>
      </div>
    </div>

    <div class="control-group">
      <%= f.label :labels, :class => 'control-label' %>
      <div class="controls">
        <%= f.text_field :labels, :class => 'text_field' %>
      </div>
    </div>

    <div class="form-actions">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to 'Cancel', labeljobs_path, :class => 'btn' %>
    </div>
  </fieldset>
<% end %>
